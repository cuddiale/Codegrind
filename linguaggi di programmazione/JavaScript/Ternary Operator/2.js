// Scrivi un codice che prenda un numero come input e verifichi se è pari o dispari utilizzando l'operatore ternario.
const numero = 10;
const risultato = numero % 2 === 0 ? "pari" : "dispari";
console.log("Il numero è:", risultato);

# Creare una nuova lista che contenga solo le chiavi del dizionario precedente.

persona = {'nome': 'Mario', 'cognome': 'Rossi', 'età': 30, 'sesso': 'M'}

d = {'a': 1, 'b': 2, 'c': 3}

keys = list(d.keys())

print(keys)

# oppure

chiavi = list(persona.keys())

print(chiavi)